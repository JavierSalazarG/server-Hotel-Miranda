// const express = require("express");

// import { Router } from "express";
// import { bookingRouter } from "./controllers/bookingsController";
// import { commentsRouter } from "./controllers/commentsController";
// import { loginRouter } from "./controllers/login";
// import { roomsRouter } from "./controllers/roomsController";
// import { usersRouter } from "./controllers/usersController";
// import { loginAuthenticationMiddleware } from "./middleware/login";

// const server = Router();
// server.use("/login", loginRouter);
// server.use(loginAuthenticationMiddleware);
// //Bookings
// server.use("/bookings", bookingRouter);
// server.use("/bookings/:id", bookingRouter);

// //Comments
// server.use("/comments", commentsRouter);
// server.use("/comments/:id", commentsRouter);

// //Rooms
// server.use("/rooms", roomsRouter);
// server.use("/rooms/:id", roomsRouter);

// //Users
// server.use("/users", usersRouter);
// server.use("/users/:id", usersRouter);

// export default server;
